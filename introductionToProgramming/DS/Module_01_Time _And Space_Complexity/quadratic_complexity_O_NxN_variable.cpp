#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++) /* the complexity of this code id - o(n) */
    {
        for (int j = 0; j < m; j++) /* the complexity of this code id - o(m) */
        {
            /* code */
            cout << "Hello" << endl;
        }
        
    }
    
    return 0;
}


// for example if the value of n is 10 and the value of is 5, than it will be n*m = 10*5 = 50